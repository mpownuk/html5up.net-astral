import { Home } from "./Home"

export const Section = () => {
    return (
        <section className="section">
            <div className="section--home">
                <Home />
            </div>
        </section>
    )
}